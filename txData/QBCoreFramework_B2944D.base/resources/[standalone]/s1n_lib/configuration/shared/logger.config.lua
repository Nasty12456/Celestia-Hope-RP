Config = Config or {}

Config.Logger = {
    -- Set this variable to false if you want to disable entirely the logger (no logs will be shown) in the F8 console and the server console
    enable = true,

    -- Set this variable to true if you want to enable the debug mode (for developers and support)
    debugMode = true
}